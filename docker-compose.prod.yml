services:
  php:
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - ./src:/var/www/html
      - ./php.ini:/usr/local/etc/php/php.ini
    networks:
      - app-network
    container_name: zitazi-php
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      TZ: Asia/Tehran

  nginx:
    image: nginx:latest
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./src:/var/www/html
      - ./nginx/production/default.conf:/etc/nginx/conf.d/default.conf
      - /root/.acme.sh/zitazi-crawler.ir_ecc:/etc/nginx/certs
    depends_on:
      - php
    networks:
      - app-network
    environment:
      TZ: Asia/Tehran

    container_name: zitazi-nginx
    restart: unless-stopped

  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: 123
      TZ: Asia/Tehran

    ports:
      - "3308:3306"
    volumes:
      - ./mysql:/var/lib/mysql
      - ./mysql-conf/conf.d:/etc/mysql/conf.d
    networks:
      - app-network
    container_name: zitazi-mysql
    restart: unless-stopped

  redis:
    image: redis:latest
    environment:
      TZ: Asia/Tehran
    ports:
      - "6380:6379"
    volumes:
      - ./redis:/data
    networks:
      - app-network
    container_name: zitazi-redis
    restart: unless-stopped
    command: redis-server --save "" --appendonly no --maxmemory 200mb --maxmemory-policy allkeys-lru
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 10s
      timeout: 5s
      retries: 5

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: zitazi-phpmyadmin
    restart: unless-stopped
    depends_on:
      - mysql
    ports:
      - "8081:80"
    environment:
      PMA_HOST: mysql
      PMA_PORT: 3306
      MYSQL_ROOT_PASSWORD: 123
      UPLOAD_LIMIT: 512M
      TZ: Asia/Tehran
    networks:
      - app-network

  loki:
    image: grafana/loki
    container_name: zitazi-loki
    ports:
      - "3100:3100"
    command: -config.file=/etc/loki/local-config.yaml
    volumes:
      - ./loki-config.yml:/etc/loki/config.yml
    networks:
      - app-network

  promtail:
    image: grafana/promtail
    container_name: zitazi-promtail
    volumes:
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      - /var/run/docker.sock:/var/run/docker.sock
      - ./promtail-config.yml:/etc/promtail/config.yml
    command: -config.file=/etc/promtail/config.yml
    depends_on:
      - loki
    networks:
      - app-network

  grafana:
    image: grafana/grafana
    container_name: zitazi-grafana
    ports:
      - "3001:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
    depends_on:
      - loki
    networks:
      - app-network


volumes:
  mysql:
  redis:

networks:
  app-network:
    driver: bridge
